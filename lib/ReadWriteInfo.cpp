//
// Created by Jon Stephens on 3/28/22.
//

#include "ReadWriteInfo.h"
#include "RegisterVal.h"

namespace vanguard {
    /*std::vector<Val> ReadWriteInfo::reads(const Instruction &ins) {
        std::vector<Val> reads;

        for(auto &use : ins.uses()) {
            reads.push_back(RegisterVal(*use.get()));
        }

        return reads;
    }
    std::vector<Val> ReadWriteInfo::writes(const Instruction &ins) {
        std::vector<Val> writes;
        writes.push_back(RegisterVal((Value &) ins));
        return writes;
    }*/
};


